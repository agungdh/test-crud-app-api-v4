logging:
  structured:
    format:
      console: logstash
    json:
      add:
        service: ${spring.application.name:test-crud-app-api-v4}
spring:
  application:
    name: test-crud-app-api-v4
  threads:
    virtual:
      enabled: true
